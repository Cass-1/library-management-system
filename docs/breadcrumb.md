# Breadcrumb

## backlog

## current commit

- [ ] add tests for userService
  - i have run into an issue of the tests not executing in the order i am expecting, i think that this has to do with my use of describe blocks and async modifiers
  - im now having the issue that the string for an id != the ObjectId of that Id. I think i may need to make a modification of my UserService.getUser so that it puts the "id" into ObjectId("id") when a search is performed

## future commit tasks

- [ ] work on the other todos and fixmes
